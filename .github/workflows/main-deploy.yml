name: Next.js CD

on:
  workflow_run:
    workflows: ['Test']
    branches: [main]
    types:
      - completed
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: Production

    strategy:
      matrix:
        node-version: [latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v3.4.0

      - name: Setup pnpm
        uses: pnpm/action-setup@v2.2.4
        with:
          version: latest

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build
        env:
          NEXT_PUBLIC_URL: ${{ secrets.NEXT_PUBLIC_URL }}
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_TMDB_URL: ${{ secrets.NEXT_PUBLIC_TMDB_URL }}
          TMDB_V3_KEY: ${{ env.TMDB_V3_KEY }}
          TMDB_V4_KEY: ${{ env.TMDB_V4_KEY }}
          COOKIE_DOMAIN: ${{ env.COOKIE_DOMAIN }}
          SITE_GITHUB_ID: ${{ env.SITE_GITHUB_ID }}
          SITE_GITHUB_SCOPE: ${{ env.SITE_GITHUB_SCOPE }}

      - name: Setup AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET }}
          aws-region: us-east-1

      - name: Deploy on AWS S3
        run: aws s3 sync . s3://www.menecucci.dev
